title List Verbose list
  
Program->model.FileSystem: Create(path:String)
Program->model.Members: Create(FileSystem)
model.Members->model.FileSystem: LoadMembers()
Program->controller.Administrator: Create(ConsoleUI, Members)
Program->controller.Administrator: Run()
controller.Administrator->view.ConsoleUI: DisplayActionMenu()
controller.Administrator->view.ConsoleUI: GetAction()
view.ConsoleUI-->controller.Administrator: Action.ListVerbose
controller.Administrator->controller.Administrator: ListVerbose()
controller.Administrator->model.Members: GetMembers()
model.Members-->controller.Administrator: ReadOnlyCollection<Member>
controller.Administrator->view.ConsoleUI: DisplayVerboseList(members:ReadOnlyCollection<Member>)

alt members contains members
    loop for each member
    view.ConsoleUI->view.ConsoleUI: DisplayMemberData(Member)
end
else there is no members
    view.ConsoleUI->view.ConsoleUI: DisplayNoMembers()
end
